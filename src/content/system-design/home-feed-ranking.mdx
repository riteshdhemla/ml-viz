---
title: "Design a Home-Feed Ranking System"
description: "A Pinterest/Netflix-style personalized home feed — a retrieval → ranking funnel that serves billions of items under a sub-200ms budget, walked through the ML project loop from data to deployment feedback."
spine: ml
spineStages: [data, hypothesis-space, evaluation]
company: "Pinterest / Netflix"
domain: "Recommendations"
scale: "300M+ users · billions of items · <200ms feed budget · 10k+ QPS"
difficulty: intermediate
relatedLessons:
  - "recommender-systems/01-the-recommendation-problem"
  - "recommender-systems/03-deep-and-two-tower"
  - "recommender-systems/06-ad-ranking-and-ctr-prediction"
estimatedMinutes: 25
---

# Design a Home-Feed Ranking System

<SystemDesignMeta
  track="ML System Design"
  company="Pinterest / Netflix"
  scale="300M+ users · billions of items · <200ms budget"
  difficulty="intermediate"
/>

"Design the home feed" is the canonical ML system-design interview. The trap is
to reach for a model on the first sentence. Strong answers move through the
**ML project loop** in order — data → hypothesis space → objective →
optimization → evaluation → feedback, and treat every design choice as *which
slot am I filling, and what breaks if I skip it?* The `SpineNav` strip above
marks the stages this problem leans on hardest. The design below is not
hypothetical — it is, to a first approximation, how Instagram Explore,
YouTube's recommender, and Twitter's For You feed are actually built, and each
stage is anchored in what those teams have published.

## 1. Clarify the problem

Before designing anything, pin down scope with the interviewer:

- **What is the feed?** A ranked, infinite-scroll list of items (pins, videos,
  posts) personalized per user, refreshed on each session.
- **What are we optimizing?** Long-term engagement (return visits, time well
  spent) — *not* raw click-through, which is easy to game with clickbait.
  YouTube made this concrete by ranking on **expected watch time** rather than
  click probability, precisely because clickbait wins clicks it can't hold
  ([Covington et al., RecSys 2016](https://dl.acm.org/doi/10.1145/2959100.2959190)).
- **Who are the users / items?** 300M+ users, billions of candidate items,
  heavy-tailed popularity, constant cold-start on both sides.
- **What's out of scope?** Ads ranking (a separate auction — see the ad-ranking
  lesson), notifications, and search.

<Callout type="tip" title="Interview signal">
Naming the objective as *long-term engagement* and immediately flagging the
proxy-metric risk (CTR → clickbait) is the single highest-signal thing you can
say in the first two minutes.
</Callout>

<ThinkFirst question="Billions of candidate items, a 200ms end-to-end budget. Before any modeling — what does the arithmetic alone force on the architecture?">
Divide the budget by the scale: at 10k QPS you can afford to run an expensive
model over a few hundred candidates per request, not billions. Something cheap
has to shrink billions down to hundreds first — the arithmetic forces a
**funnel** before you've picked a single model.
</ThinkFirst>

## 2. Requirements & scale

| Dimension | Target | Consequence for the design |
|---|---|---|
| Latency | < 200 ms p99 end-to-end | Rules out scoring billions of items directly: forces a funnel |
| Throughput | ~10k feed requests/sec at peak | Ranker must be cheap per item; heavy work goes offline |
| Freshness | New items rankable within minutes | Streaming feature pipeline, not nightly batch |
| Candidate pool | Billions of items | Approximate nearest-neighbor retrieval, not brute force |
| Availability | 99.9% | Graceful fallback to a non-personalized feed |

**Back-of-envelope:** 10k QPS × 200ms budget means each request scores maybe a
few hundred candidates through a heavy model, not millions. That single
constraint dictates the whole architecture: a **funnel**. For calibration on
what the top end looks like: Meta reports that Instagram's recommendation
systems extract **65 billion features and make 90 million model predictions
every second**
([Meta Engineering](https://engineering.fb.com/2023/08/09/ml-applications/scaling-instagram-explore-recommendations-system/)).

## 3. ML problem framing

The feed is not one model — it's a **multi-stage retrieval → ranking** pipeline,
each stage a different ML slot:

1. **Retrieval** *(hypothesis space)*: from billions of items, cheaply fetch
   ~1000 candidates likely to be relevant. Framed as **approximate nearest
   neighbor** search in an embedding space.
2. **Ranking** *(hypothesis space + objective)*: score those ~1000 candidates
   with an expensive model predicting `P(engage)`, framed as **learning to
   rank** / pointwise CTR-style prediction with an engagement objective.
3. **Re-ranking**: apply business logic: diversity, freshness, dedup, policy.

Production systems make the funnel even deeper. Instagram Explore retrieves
thousands of candidates from many sources, cuts them to hundreds with a
**lightweight first-stage ranker**, and only then pays for the heavy model;
YouTube's classic design narrows millions of videos to hundreds in candidate
generation and ranks only dozens at the final stage. Each extra stage buys the
next one a smaller, better pool.

*What was breaking before?* A single ranker over all items blows the latency
budget; pure popularity ignores personalization. The funnel exists to make
personalized scoring **tractable** under the budget.

<ThinkFirst question="Nobody rates their feed. Where do the training labels come from — and what's the catch?">
Implicit feedback: impressions, clicks, saves, dwell, hides. Abundant but
biased — users can only engage with what the *old* model chose to show them, so
naive training just relearns yesterday's policy. You have to correct for the
logging policy (propensity weighting, a randomized-exposure slice) or the loop
feeds on itself.
</ThinkFirst>

## 4. Data & features *(spine: data)*

- **Labels:** implicit feedback — impressions, clicks, saves, long dwell,
  hides. Weight positives by engagement depth; treat impressions-without-action
  as negatives (with care around position bias).
- **Features:** user (long/short-term interest embeddings), item (content
  embedding, age, popularity), and **context** (time, device, session so far).
- **Feature store:** the *same* pipeline computes features for training and
  serving to avoid **training-serving skew** — the classic production bug where
  offline metrics look great and online performance tanks.
- **Freshness:** velocity/recency features stream in near-real-time so a
  minutes-old viral item can surface. A subtle trick from YouTube: include the
  **age of the training example** as a feature, so the model can separate
  "this item is good" from "this item was new when logged" — without it, the
  model implicitly ranks by historical popularity and demotes everything fresh.

<Callout type="warning" title="Position & popularity bias">
Logged feedback is biased by what the *old* model showed. Correct for it —
inverse-propensity weighting, or reserving a small randomized-exposure slice —
or the model just relearns its own past behavior.
</Callout>

<ThinkFirst question="Retrieval has ~40ms to find 1000 relevant items among billions. What shape must the model take for that to even be possible?">
User and item can't interact *inside* the model at query time — that would mean
scoring billions of pairs per request. Encode them **separately**, so item
embeddings are precomputed and indexed offline, and query-time work collapses
to one user embedding plus an approximate nearest-neighbor lookup. That
constraint is exactly why the two-tower architecture exists, and why
cross-attention retrieval is off the table.
</ThinkFirst>

## 5. Model *(spine: hypothesis space + objective + optimization)*

**Retrieval: the two-tower model.** A user tower and an item tower each map to
the same $d$-dimensional space; relevance is a dot product:

$$
s(u, i) = \mathbf{e}_u^\top \mathbf{e}_i
$$

Item embeddings are precomputed offline and indexed in an ANN index; at request
time we embed the user once and query the index. Start from a **matrix
factorization** baseline to build intuition, then replace the towers with deep
encoders. Train with **in-batch negatives** — Pinterest's home-feed two-tower
uses batch sizes around 6000 so every positive gets thousands of negatives for
free ([Pinterest Engineering](https://medium.com/pinterest-engineering/pinterest-home-feed-unified-lightweight-scoring-a-two-tower-approach-b3143ac70b55)).

<MatrixFactorizationViz />

**Ranking: a deep multi-task model.** The ~1000 retrieved candidates go
through a richer model (DLRM-style: sparse ID embeddings + feature crosses +
MLP, where the embedding tables dominate the parameter count —
[Naumov et al., 2019](https://arxiv.org/abs/1906.00091)). Production rankers
are **multi-task**: one shared network with heads for `P(click)`, `P(save)`,
`P(share)`, `P(hide)`, combined into a single score by a hand-tuned value
formula. Twitter's open-sourced For You ranker made its weights public — a
reply that draws the author back into conversation is worth on the order of
**100× a like**, and a report is a large negative
([twitter/the-algorithm](https://github.com/twitter/the-algorithm)), so the
value formula, not the model, is where product strategy lives. Baseline:
logistic regression on hand-crafted features → prove the pipeline end-to-end
before adding depth.

For the retrieval index, the ANN structure itself is worth knowing cold:

<WikiLink slug="hnsw" title="HNSW — the graph index behind fast retrieval" />

<ThinkFirst question="Peak traffic, and the heavy ranker breaches its 80ms budget on 5% of requests. What do those users see — an error, a spinner, or something else?">
Neither. The funnel doubles as a **degradation ladder**: serve the first-stage
(lightweight) scores without the heavy re-rank, fall back to a
popularity-plus-recency feed, or replay the user's last cached ranking. Each
rung trades personalization quality for availability, which is why 99.9%
availability was a requirements-table row, not an afterthought. An empty feed
costs you the session; a slightly worse feed costs you almost nothing.
</ThinkFirst>

## 6. Serving & scaling

Latency budget breakdown (of the 200ms):

| Stage | Budget | How |
|---|---|---|
| Feature fetch | ~30 ms | Feature store, cached user features |
| Retrieval (ANN) | ~40 ms | HNSW over precomputed item embeddings; multiple sources merged |
| Ranking | ~80 ms | Batch-score ~1000 candidates on GPU/optimized CPU |
| Re-rank + assemble | ~20 ms | Diversity, dedup, policy filters |

Cache aggressively (user embeddings, item metadata); precompute item embeddings
in a nightly + streaming hybrid; degrade gracefully to a popularity feed if the
ranker times out.

<WikiLink slug="vector-databases" title="Vector databases — serving the ANN index" />

<ThinkFirst question="Your new ranker lifts offline NDCG by 3%. Do you ship it?">
Not yet. Offline gains are computed on logged data the *old* model produced —
position bias, training-serving skew, and proxy-metric mismatch can each fake a
win. Ship behind an **A/B test** on the real objective (long-term engagement)
with guardrail metrics, and let that (not NDCG) be the judge.
</ThinkFirst>

## 7. Evaluation *(spine: evaluation)*

- **Offline:** ranking metrics — NDCG@k, Recall@k for retrieval, AUC/logloss for
  the ranker. Offline gains are necessary but *not sufficient*.
- **Online:** the real judge is an **A/B test** on engagement, with
  **guardrail metrics** (dwell quality, hide rate, creator diversity, latency)
  so you don't ship a CTR win that quietly harms the ecosystem. Netflix runs
  its recommender A/Bs against **member retention and medium-term engagement**
  and credits personalization with over **$1B/year** of retained value
  ([Gomez-Uribe & Hunt, 2016](https://dl.acm.org/doi/10.1145/2843948)) — the
  online judge is a business metric, not a ranking metric.
- **Counterfactual:** off-policy estimators (IPS) to screen candidates before
  spending A/B traffic.

<ThinkFirst question="Thought experiment: you freeze everything — no retrains, no embedding refreshes — for 90 days. Offline metrics on the old test set don't move. What actually decays in production, and in what order?">
Retrieval rots first: items uploaded after the freeze have no learned
embeddings, so the fresh half of the corpus is invisible within days. Then the
ranker's feature distributions drift (seasonality, trends, new creators) and
its calibration goes with them. The old test set can't detect any of this
because it's drawn from the old world. The feed degrades into a greatest-hits
archive of the quarter you froze, which is why retraining cadence is part of
the architecture, not an ops detail.
</ThinkFirst>

## 8. Monitoring & feedback *(spine: feedback)*

- **Drift:** monitor feature distributions and prediction distributions; user
  interests and item inventory shift constantly.
- **Retraining:** ranker retrained daily (or continuously); embeddings
  refreshed as new items arrive. The loop closes back into **data**.
- **Cold start:** new items get content-based embeddings + an exploration boost;
  new users get a popularity/onboarding feed until signal accrues.

<WikiLink slug="cold-start-problem" title="Cold start — bootstrapping new users & items" />

## 9. Tradeoffs & alternatives

| Choice | Alternative | Why the funnel wins here |
|---|---|---|
| Two-stage funnel | One big ranker over all items | Latency budget makes full scoring impossible |
| Two-tower retrieval | Cross-attention retrieval | Towers allow precomputed item embeddings + ANN; cross-attention can't scale to billions |
| Implicit feedback | Explicit ratings | Implicit is abundant; explicit is sparse and biased |
| Daily retrain | Fully online learning | Simpler, more stable; online learning adds infra + failure modes |

## 10. Interviewer follow-ups

<Details summary="How do you stop the feed from becoming a filter bubble?">
Add an explicit **diversity** objective in re-ranking (e.g. determinantal point
processes or a simple category/creator quota), and reserve exploration slots.
Track creator- and topic-diversity as guardrail metrics in every A/B test so a
pure-engagement win that collapses diversity is caught before launch.
</Details>

<Details summary="A viral item is 10 minutes old — how does it get ranked?">
Streaming features (velocity, recency) feed the ranker within minutes, and the
item gets a content-based embedding at ingest so retrieval can surface it before
it has any engagement history. An exploration boost gives it initial impressions
to gather signal.
</Details>

<Details summary="Offline NDCG went up but the A/B test is flat. What happened?">
Usual suspects: training-serving skew (features differ online), position/
popularity bias the offline metric rewards but users don't, objective mismatch
(you optimized CTR, users care about dwell), or the offline eval leaked future
information. Check feature parity first.
</Details>

## Key takeaways

- The **latency budget is the architecture**: it forces a retrieval → ranking
  funnel, not a single model.
- **Two-tower retrieval** makes personalized scoring tractable by precomputing
  item embeddings and querying an ANN index.
- **Optimize long-term engagement**, guard the proxy metrics, and let the **A/B
  test** — not offline NDCG — be the final judge.
- The whole thing is a **loop**: production feedback becomes tomorrow's training
  data, so drift monitoring and retraining are part of the design, not an
  afterthought.

## References

- [Scaling the Instagram Explore recommendations system](https://engineering.fb.com/2023/08/09/ml-applications/scaling-instagram-explore-recommendations-system/): Meta Engineering, 2023; the production multi-stage funnel, Two Towers retrieval, and the 90M-predictions/second serving numbers.
- [Deep Neural Networks for YouTube Recommendations](https://dl.acm.org/doi/10.1145/2959100.2959190): Covington et al., RecSys 2016; the canonical candidate-generation → ranking design, the watch-time objective, and the example-age trick.
- [Twitter's Recommendation Algorithm](https://github.com/twitter/the-algorithm): X/Twitter open-source release, 2023; the For You heavy ranker's multi-head engagement weights.
- [Deep Learning Recommendation Model for Personalization and Recommendation Systems](https://arxiv.org/abs/1906.00091): Naumov et al., Meta, 2019; the embedding-table-dominated ranking architecture (DLRM).
- [The Netflix Recommender System: Algorithms, Business Value, and Innovation](https://dl.acm.org/doi/10.1145/2843948): Gomez-Uribe & Hunt, ACM TMIS 2016; A/B testing on retention and the business value of personalization.
- [Pinterest Home Feed Unified Lightweight Scoring: A Two-tower Approach](https://medium.com/pinterest-engineering/pinterest-home-feed-unified-lightweight-scoring-a-two-tower-approach-b3143ac70b55): Pinterest Engineering, 2021; two-tower training with in-batch negatives and embedding serving.
