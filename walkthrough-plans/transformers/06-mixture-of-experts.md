# Plan — transformers/06 (algorithm). Top-k routing, sparse combination, N-vs-k scaling, collapse +
balance loss. Added: intuition; validation — k=N sparse MoE == dense softmax mixture (exact) + eval
counts scale with k; Mixtral-scale memory-vs-compute demo (45B stored / 11B active); NEW key takeaways.
