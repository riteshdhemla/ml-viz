# Plan — transformers/05 (concept). Chinchilla law, fit from sweep, optimal allocation, frontier plot.
Added: intuition; validation — EXACT derivative-based N* == brute force (15.2B), exposing that the
notebook's sqrt(C/120) rule assumes alpha=beta (28.9B, ~2x off for the fitted exponents, 72 tok/param);
flat-optimum demo (4x deviation ~1.5% loss -> Llama over-training strategy), made self-contained vs the
plot cell's C-array shadowing; gotchas.
