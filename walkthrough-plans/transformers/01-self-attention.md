# Plan — transformers/01 (algorithm). Attention from scratch, hand-verified, sqrt(dk) variance check,
viz, causal mask, exercises + DML. Added: intuition; library — jax einsum+softmax == ours; gotchas
(softmax saturation demo: unscaled entropy 0.003 vs scaled 1.31; permutation-invariance demo).
