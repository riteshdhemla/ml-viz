# Plan — transformers/04 (algorithm). Cross-attention shapes, KV cache MHA/GQA/MQA, RoPE (relative
property asserted), Flash memory, sliding window. Added: intuition; validation — tiled online-softmax
attention == standard (1e-15, THE FlashAttention identity) + buggy no-rescale variant demo (silent 0.56
error); per-figure takeaways; gotchas.
