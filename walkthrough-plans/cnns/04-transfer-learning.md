# Plan — cnns/04 (applied). Strategies, torchvision sketch, ResNet param math, discriminative LR.
FIXED: duplicate Key-takeaways cell (dropped the shorter one). Added: intuition; library — the
torchvision freeze/swap pattern + verified param fractions (0.016% / 58.6% / 100%); gotchas
(catastrophic forgetting, domain gap, frozen BN, negative transfer); per-figure takeaways.
