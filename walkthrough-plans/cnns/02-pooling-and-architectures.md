# Plan — cnns/02 (algorithm). max_pool, receptive field, VGG 3x3, max-vs-avg, exercises+toy CNN.
Added: intuition; library — max_pool == jax.lax.reduce_window (==nn.MaxPool2d); gotchas (pooling
loses location, strided-conv replacement); per-figure takeaways. Kept takeaways+exercises.
