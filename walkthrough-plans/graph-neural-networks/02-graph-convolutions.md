# Plan — gnn/02 (algorithm). Symmetric normalization, lesson's 3-node trace, 2-layer GCN, GraphSAGE
sampling. Added: intuition; validation — spectrum of A_hat in (-1,1] vs raw A+I max eig 3.3; A_hat^10
stable vs (A+I)^10 explodes (self-contained Xs); sampling-variance gotcha (1/sqrt(k)); notes.
