# Plan — gnn/03 (algorithm). GAT over neighbors, readouts, over-smoothing distance collapse.
Added: intuition; validation — GAT with a=0 == mean aggregation exactly (attention strictly
generalizes GCN); residual-connection mitigation demo (distances decay far slower); notes.
