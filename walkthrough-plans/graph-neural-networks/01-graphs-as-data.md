# Plan — gnn/01 (algorithm). Adjacency+features, mean message passing, receptive field, permutation
equivariance. Added: intuition; validation — over-smoothing == power iteration to the random-walk
stationary distribution (limit == degree-weighted feature average, asserted); aggregator-blind-spots
gotcha (mean can't tell {2,2} from {1,3}; only sum sees a 3rd identical neighbor -> GIN); notes.
