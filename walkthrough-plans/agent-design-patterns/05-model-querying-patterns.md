# Plan — agent-design-patterns/05-model-querying-patterns (applied/systems flex)
From-scratch one-shot vs incremental querier + error recovery + exercise. Retrofit: intuition + gotchas + key takeaways.
Validation: incremental threads scratchpad w/ retry budget; one-shot vs incremental cost tradeoff (1 vs 4 calls); retries-beat-one-shot reliability demo.
Note: RetryQuerier exercise needs the learner's solution (harness can't auto-solve); concept + validations run clean.
