# Plan — agent-design-patterns/04-planning-patterns (applied/systems flex)
From-scratch single/multi-path plan generation + scoring + exercise. Retrofit: intuition + gotchas + key takeaways.
Validation: composite score == weighted sum & selects argmax plan; multi-criteria-reranking demo.
FIXED pre-existing bug: plan-tree viz used plt.FancyBboxPatch (nonexistent) -> mpatches.FancyBboxPatch; notebook crashed before.
Note: exercise has multiple scaffolds (harness can't auto-solve); concept + validations run clean.
