# Plan — building-with-llms/13-voice-and-multimodal-ai (applied flex)
From-scratch: voice latency + CLIP retrieval + exercise. Retrofit: intuition + validation + gotchas + key takeaways.
Validation: pipeline latency=sum, p95 tail; cosine exact-match retrieval; clustered cross-modal demo (toy uses random vecs).
