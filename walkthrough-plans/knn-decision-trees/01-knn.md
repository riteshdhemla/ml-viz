# Plan — knn-decision-trees/01-knn (algorithm)
KNN class, decision boundary, effect of k, distance metrics, curse of dimensionality, exercises.
## Added: intuition; library (placed before the distance-metrics cell which reuses `y` as a local
3-vector) — sklearn KNeighborsClassifier == our KNN (100% grid agreement); feature-scaling gotcha
(self-contained: rescaling flips the nearest neighbor); per-figure takeaways. Kept takeaways+exercises.
