# Plan — ensemble-methods/03 (algorithm). GBM from scratch (fit residuals), shrinkage, early stopping.
Added: intuition; library — our GBM test MSE == sklearn GradientBoostingRegressor (0.012 vs 0.013);
early-stopping gotcha (eta=1 best at tree 9 then climbs); gotchas (XGBoost leaf weight -G/(H+lam),
tabular-first); NEW key takeaways. Fixed a literal {best} placeholder.
