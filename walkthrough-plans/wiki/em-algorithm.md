# Plan — wiki/em-algorithm (unsupervised)
EM for GMM from scratch. Retrofit: intuition + 1 validation + gotchas + takeaways.
Validation: Monotonic log-lik ascent + component separation. Gotcha demo: singularity (sigma->0 on a point diverges likelihood). Note: notebook data too separated for local-optima demo, so used singularity instead.
