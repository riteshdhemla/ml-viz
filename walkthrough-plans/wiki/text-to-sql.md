# Plan — wiki/text-to-sql (llm)
Schema linking + SQLite execution from scratch. Retrofit: intuition + 2 validations + gotchas + takeaways.
Validation: Schema linking finds tables; query executes. Gotcha demo: execution accuracy not exact-match (equivalent queries differ textually).
