# Plan — wiki/langgraph (agents)
StateGraph + reducers + agent loop from scratch. Retrofit: intuition + 2 validations + gotchas + takeaways.
Validation: Calculator tool works; append reducer preserves history + terminates. Gotcha demo: overwrite reducer destroys conversation history.
