# Plan — wiki/perceptron-learning (foundations)
Perceptron + MLP-for-XOR from scratch. Retrofit: intuition + 2 validations + gotchas + takeaways.
Validation: Learns AND; MLP solves XOR. Gotcha demo: perceptron never converges on XOR. FIXED pre-existing bug: MLP-forward print formatted tuples with numeric spec -> TypeError (added !s). NOTE: OR/mistake-bound exercises hit a harness substitution-ordering limit.
