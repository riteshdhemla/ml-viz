# Plan — wiki/neural-radiance-fields (generative-models)
Positional encoding + volume rendering from scratch. Retrofit: intuition + 2 validations + gotchas + takeaways.
Validation: PE dim 1+2L; render weights valid + occlusion. Gotcha demo: spectral bias (raw MLP mse 0.046 >> PE 8e-5).
