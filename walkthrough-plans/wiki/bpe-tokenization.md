# Plan — wiki/bpe-tokenization (nlp)
BPE pair-merge training + tokenize from scratch. Retrofit: intuition + 2 validations + gotchas + takeaways.
Validation: Lossless round-trip; merges shorten avg sequence. Gotcha demo: no OOV (char fallback). NOTE: first_merge exercise solution <details> after assert -> harness cannot substitute (concept+validations pass).
