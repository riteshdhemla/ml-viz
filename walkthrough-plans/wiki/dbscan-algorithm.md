# Plan — wiki/dbscan-algorithm (clustering)
region_query + DBSCAN from scratch. Retrofit: intuition + 1 validation + gotchas + takeaways.
Validation: Matches sklearn; isolated=noise, dense=core. Gotcha demo: eps too small=all noise / too large=one cluster. NOTE: multiple learner exercises -> harness "more scaffolds than solutions" (concept+validations pass).
