# Plan — wiki/imitation-learning (reinforcement-learning)
Expert + BC + DAgger from scratch. Retrofit: intuition + 2 validations + gotchas + takeaways.
Validation: Expert converges; BC recovers gain; DAgger converges + wider state coverage. Honest: linear task lets BC extrapolate (no failure). FIXED pre-existing exercise assert (claimed wide-BC beats narrow; both ~0 on linear task).
