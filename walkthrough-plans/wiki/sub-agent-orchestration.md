# Plan — wiki/sub-agent-orchestration (agents)
Sequential/Parallel/Loop orchestration from scratch. Retrofit: intuition + 1 validation + gotchas + takeaways.
Validation: Sequential latency=sum, parallel=max, same cost. Gotcha demo: parallel latency = slowest branch (critical path).
