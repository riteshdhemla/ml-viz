# Plan — pca-dimensionality/01 (algorithm). PCA via eigendecomp, why PC1=top eigenvector, scree.
Added: intuition; library (before cell 7 clobbers X) — our eigendecomp PCA == sklearn PCA (variance+
directions up to sign); scale-sensitivity gotcha (raw PC1 1.0 vs standardized 0.5); per-figure takeaways.
