# Plan — pca-dimensionality/03 (algorithm). Digit compression, reconstruction vs m, eigendigits, anomaly.
Added: intuition; library — our reconstruct == sklearn inverse_transform(transform); anomaly-detection
gotcha (noise reconstructs 5x worse); per-figure takeaways; NEW key takeaways. Kept exercises.
