# Plan — neural-networks/03-layers-and-forward-pass (concept)
Layer equation, reusable forward pass, width/depth surfaces, takeaways, exercises.
## Added: intuition; per-figure takeaways; library — jax forward pass == numpy forward (bit-match);
gotchas (W vs W.T shapes, no output activation, param count scaling demo). Kept takeaways+exercises.
