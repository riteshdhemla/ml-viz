# Plan — neural-networks/04-xor-and-mlp (concept)
Perceptron rule, XOR not separable, MLP solves XOR, softmax/cross-entropy, temperature.
## Fixed bug: cell 7 nested same-quote f-string (Py3.11 SyntaxError -> Colab crash). ## Added:
intuition; per-figure takeaways; library — sklearn Perceptron(0.5) vs MLPClassifier(1.0) on XOR,
scipy.special.softmax == ours; gotchas (XOR needs hidden layer, softmax max-subtraction/overflow,
double-softmax, temperature). Kept takeaways+exercises.
