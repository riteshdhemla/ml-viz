# Plan — neural-networks/05-batchnorm-and-dropout (concept)
BatchNorm class, inverted dropout, activation-distribution effect, ensemble view, exercises.
## Added: intuition; per-figure takeaways; library — BatchNorm(gamma=1,beta=0) == sklearn
StandardScaler; gotchas (train/eval modes, small-batch noise demo [batch-mean std ~1/sqrt(bs)],
dropout train-only, BN+dropout interaction). Kept takeaways+exercises.
