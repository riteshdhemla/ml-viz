# Plan — neural-networks/01-what-is-a-neuron (algorithm/concept)
Already rich (neuron, activations, non-linearity proof, 2-layer MLP + hand backprop, takeaways,
exercises + activation bank). ## Added: intuition; per-figure takeaways (activations, single-neuron
line, MLP ring boundary); library — LogisticRegression fails on rings (~0.6) while our MLP == sklearn
MLPClassifier (~1.0); gotchas (linear collapse demo, saturation, dead ReLU, scale) ; kept existing
Key takeaways + exercises.
