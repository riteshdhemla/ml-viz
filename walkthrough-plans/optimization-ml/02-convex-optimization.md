# Plan — optimization-ml/02-convex-optimization (concept)
Convexity viz, Jensen, convex logistic loss, non-convex 2-neuron loss.
## Added: intuition (convex=one global min); library way — scipy.optimize.minimize_scalar
finds the global min, cross-checked vs a wide fine grid (data near-separable, min ~6.8);
non-convex GD demo (two inits -> two symmetric minima) as gotcha; per-figure takeaways;
key takeaways. Exercises kept.
