# Plan — optimization-ml/04-loss-functions (concept)
Regression losses viz, MSE->mean/MAE->median, cross-entropy. ## Added: intuition (loss=noise
model); library — sklearn.log_loss == our BCE, scipy.optimize confirms MSE-min=mean/MAE-min=median;
gotchas (MSE outliers, MAE non-diff, CE log(0), train-loss vs eval-metric) + grad-magnitude demo;
per-figure takeaways; recap. Exercise kept.
