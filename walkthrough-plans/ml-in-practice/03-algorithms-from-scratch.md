# Plan — ml-in-practice/03-algorithms-from-scratch (algorithm flex)
Linear regression (NE vs GD), K-Means, decision-tree Gini split, from scratch. Retrofit: intuition + 3 validations + gotchas + key takeaways.
Validation: NE==GD converge to same coeffs; K-Means inertia monotone + recovers clusters; best-split finds perfect cut. Gotcha demo: K-Means local optima (k=5 on 3 blobs).
