# Plan — rnns/04 (algorithm). SSM recurrence==convolution duality, O(L) vs O(L^2).
Added: intuition; library — scipy fftconvolve == recurrence (the actual S4 training path); stability
gotcha (|A|<1 decays, 1.01 explodes, kernel visible); per-figure takeaways; NEW key takeaways.
