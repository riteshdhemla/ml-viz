# Plan — rnns/03 (algorithm). LSTM cell from scratch, hand-verified step, param counts, gradient
highway, GRU, memory-survives plot. Added: intuition; library — jax LSTM-step == ours; forget-bias
init gotcha (bias 0 keeps 1e-6 vs bias 2 keeps 8% after 20 steps); per-figure takeaways. Kept all.
