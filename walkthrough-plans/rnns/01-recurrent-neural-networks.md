# Plan — rnns/01 (algorithm). VanillaRNN, hand-verified forward, param count, parity task, exercises.
Added: intuition; library — jax lax.scan == our loop (h and y exact); memory-fade gotcha (first-input
effect 3.94 -> 0.03 over 24 steps, measured); per-figure takeaways. Kept takeaways+exercises+DML 54.
