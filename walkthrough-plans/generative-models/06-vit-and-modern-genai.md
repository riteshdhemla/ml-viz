# Plan — generative-models/06 (applied). ViT patch embed, CFG, CycleGAN cycle loss, latent diffusion
ratio, SD pipeline shapes. Added: intuition; validation — vectorized patching == explicit slicing
(+ wrong-transpose scrambling demo: same shape, wrong content); CFG 2x-cost gotcha; per-figure takeaways.
