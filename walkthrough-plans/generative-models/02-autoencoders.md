# Plan — generative-models/02 (algorithm). AE from scratch on swiss roll, latent viz, holes demo.
Added: intuition; validation — AE error >= PCA rank-2 floor (Eckart-Young) and close to it;
no-bottleneck identity demo (latent 3 -> MSE 0.0005, epochs raised to 5000 so it actually collapses);
per-figure takeaways.
