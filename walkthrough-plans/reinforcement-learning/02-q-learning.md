# Plan — reinforcement-learning/02-q-learning (algorithm flex)
From-scratch tabular Q-learning + off-policy + Key takeaways + DML bank.
## Added
0. Intuition (model-free; TD bootstrap; off-policy max).
2. Validation — model-free Q-learning matches model-based value iteration EXACTLY
   (policy 24/24, ΔV≈0).
4. Gotchas — α stability (overshoot demo at α>1), ε decay, coverage, max-bias, tabular-only.
Kept Key takeaways + DML #133/#158/#161 exercises. ✅ 5 asserts.
