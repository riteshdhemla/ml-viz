# Plan — linear-regression/02-logistic-regression (algorithm)
Sigmoid/log-odds, cross-entropy, GD logistic, decision boundary, exercises.
## Added: intuition; library — sklearn LogisticRegression(C=1e12) matches (acc + weight cosine ~1);
perfect-separation gotcha (||w|| grows unboundedly); per-figure takeaways. Kept takeaways+exercises.
