# Plan — clustering/01 (algorithm). K-means from scratch, why update=mean, elbow, silhouette.
Added: intuition; library (before cell 9 clobbers X) — our k-means == sklearn KMeans (ARI 1.0);
two-moons failure gotcha (ARI 0.23, motivates DBSCAN); per-figure takeaways. Kept takeaways+exercises.
