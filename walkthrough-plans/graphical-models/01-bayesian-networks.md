# Plan — graphical-models/01 (algorithm). Sprinkler network, enumeration query, explaining away, param savings.
Added: intuition; library — exact enumeration == forward-sampling Monte Carlo (0.708/0.320 both);
exponential-enumeration gotcha; per-figure takeaways. Kept takeaways+exercises.
