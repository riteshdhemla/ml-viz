# Plan — graphical-models/03 (algorithm). Markov warm-up, forward, Viterbi, forward-backward.
Added: intuition; validation (placed after viterbi def) — forward == brute-force sum over all 16 paths,
Viterbi == brute-force argmax; underflow gotcha (raw likelihood hits exactly 0.0 by T=700); per-figure
takeaways. Kept takeaways+exercises.
